linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
linter-settings:
  gosec:
    excludes:
      - G104
run:
  timeout: 5m
  tests: true
